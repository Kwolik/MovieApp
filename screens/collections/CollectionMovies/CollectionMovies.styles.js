import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  menu: {
    marginBottom: 10,
  },
  poster: {
    width: 160,
    height: 256,
    borderRadius: 10,
  },
});

export default styles;
