import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  menu: {
    flex: 1,
  },
  black: {
    marginBottom: 100,
    marginTop: 10,
  },
  load: {
    position: "absolute",
    left: "50%",
    right: "50%",
    top: "50%",
    bottom: "50%",
  },
});

export default styles;
